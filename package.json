{
  "name": "critic-markup",
  "version": "2.0.0",
  "description": "CriticMarkup in JavaScript",
  "author": "Márton Visnovitz <vimtaai@pm.me>",
  "license": "MIT",
  "repository": "github:vimtaai/critic-markup",
  "type": "module",
  "main": "src/index.js",
  "files": [
    "src/**/*.js",
    "!src/**/*.test.js"
  ],
  "scripts": {
    "lint": "eslint 'src/**/*.js'",
    "format": "prettier --check 'src/**/*.js'",
    "test": "node --test",
    "test:watch": "node --watch --test",
    "check:ci": "npm run format && npm run lint"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2"
  }
}
